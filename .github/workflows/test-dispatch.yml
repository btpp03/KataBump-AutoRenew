name: Dispatch Test
on:
  workflow_dispatch:
  repository_dispatch:
    types: [test, renew]
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "event = ${{ github.event_name }} dispatch works"
