name: Dispatch Test
on:
  push:
    paths: ['TRIGGER.txt']
    branches: [main]
  workflow_dispatch:
  repository_dispatch:
    types: [test, renew]
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "event = ${{ github.event_name }} works"
